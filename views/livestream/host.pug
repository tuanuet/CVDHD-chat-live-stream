extends ../layout

block content
  h1 Welcome to Live Chat
  p Hệ thống live chat and live chat.
  hr
  .row
    .col-lg-12
      p
        | Room: #{roomId} có
        span#number-people  1
        |  người
      video#video.myvideo(autoplay='true' muted)
block js
  script.
    window.DATA_RENDER = {};
    window.DATA_RENDER.roomId = '#{roomId}';

  script(src='/socket.io/socket.io.js')
  script(src='/js/livestream/host.js')
